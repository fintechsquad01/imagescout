
import { ImageFile, ScoredImage } from '@/types/scoring';
import { VisionApiData } from '@/types/vision';

// Re-export types for backward compatibility
export type { ImageFile, ScoredImage, VisionApiData };
